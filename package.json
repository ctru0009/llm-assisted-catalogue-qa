{
  "name": "llm-assisted-catalogue-qa",
  "private": true,
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "eval": "tsx scripts/eval.ts",
    "validate:n8n": "tsx scripts/validate-n8n.ts",
    "demo": "tsx scripts/demo.ts"
  }
}
